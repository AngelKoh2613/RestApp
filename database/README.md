# Base de datos

Esta carpeta contendrá el esquema y los datos iniciales de PostgreSQL del sistema POS.

- `migrations/` — cambios versionados del esquema.
- `seeds/` — datos iniciales.
- `scripts/` — utilidades de mantenimiento.

Aún no hay tablas ni migraciones SQL.
